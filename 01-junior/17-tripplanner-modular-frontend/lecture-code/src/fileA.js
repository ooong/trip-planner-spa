






// if we just require files in our index.html whatever we define on the 'file' scope is actually put on the window scope in the browser
	// var password = "Happy Thanksgiving!";

// module.exports = password; // this works

// exports.password = password; // this works (differently than above)

// exports = password; // this does NOT work